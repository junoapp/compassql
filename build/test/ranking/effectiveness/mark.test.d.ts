export interface Rule {
    name: string;
    items: string[];
}
export declare const CC_RULESET: {
    name: string;
    rules: Rule[];
};
export declare const CD_RULESET: {
    name: string;
    rules: Rule[];
};
export declare const TT_RULESET: {
    name: string;
    rules: Rule[];
};
export declare const TD_RULESET: {
    name: string;
    rules: Rule[];
};
export declare const DD_RULESET: {
    name: string;
    rules: Rule[];
};
